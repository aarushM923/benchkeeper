# Postgres connection string. Neon: use the pooled connection string.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"

# Optional: a separate, disposable database for the integration tests
# (tests/*.db.test.ts). They are skipped when this is unset. They WIPE it.
TEST_DATABASE_URL=""
